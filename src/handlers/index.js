const handleCamera = require('./handleCamera')
const handleThermostat = require('./handleThermostat')
const handleNestError = require('./handleNestErrors')

module.exports = { handleCamera, handleThermostat, handleNestError }
