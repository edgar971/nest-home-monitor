{
  "name": "nest-home-monitor",
  "version": "1.0.0",
  "description": "Nest Home data",
  "main": "main.js",
  "author": "Edgar Pino ",
  "license": "MIT",
  "scripts": {
    "test": "mocha --recursive --reporter spec ./tests/**/*.specs.js  ./tests/*.specs.js",
    "test:watch": "yarn test --watch --watch-extensions js",
    "format": "prettier --config ./.prettierrc --write 'src/**/*.js' 'tests/**/*.js'"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "prettier": "1.16.3",
    "sinon": "^7.2.3",
    "sinon-chai": "^3.3.0"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "influx": "^5.0.0-alpha.4"
  }
}
